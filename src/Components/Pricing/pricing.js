import React from 'react';
import './pricing.css'
import Container from '@material-ui/core/Container';

const Pricing = () => (
<Container maxWidth={false} className="pricing">
I am the Pricing.
</Container>
)

export default Pricing;